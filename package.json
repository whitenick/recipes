{
  "name": "nicks-sarahs-kitchen",
  "version": "2.0.0",
  "description": "Nick's personal recipe collection — Vite build",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:data": "node build.js",
    "test": "node --test test/*.test.js"
  },
  "private": true,
  "devDependencies": {
    "marked": "^15.0.0",
    "vite": "^6.0.0"
  }
}